const projectsRoot = document.querySelector("#projects");
const search = document.querySelector("#search");
const count = document.querySelector("#project-count");
const element = (tag, className, text) => { const node = document.createElement(tag); if (className) node.className = className; if (text !== undefined) node.textContent = text; return node; };
const tag = (text) => element("span", "tag", text);
const cardImage = (project) => { if (!project.banner) return element("div", "mod-card__banner placeholder-banner"); const image = element("img", "mod-card__banner"); image.src = project.banner; image.alt = ""; return image; };
const projectIcon = (project) => { if (!project.icon) return element("span", "project-icon project-icon--fallback", project.name.slice(0, 1)); const image = element("img", "project-icon"); image.src = project.icon; image.alt = ""; return image; };
const renderProject = (project) => {
  const link = element("a", "mod-card"); link.href = `/mod.html?project=${encodeURIComponent(project.slug)}`; link.append(cardImage(project));
  const body = element("div", "mod-card__body"); const heading = element("div", "mod-card__heading"); heading.append(projectIcon(project)); const text = element("div"); text.append(element("h2", "", project.name)); const latest = project.releases[0]; if (latest) text.append(element("p", "mod-card__version", `最新版本 ${latest.version}`)); heading.append(text); body.append(heading);
  if (project.description) body.append(element("p", "mod-card__description", project.description)); const meta = element("div", "tags"); if (latest) [...latest.gameVersions.map((version) => `游戏 ${version}`), ...latest.loaders].forEach((value) => meta.append(tag(value))); if (meta.childElementCount) body.append(meta); link.append(body); return link;
};
const searchableText = (project) => [project.name, project.description, ...project.releases.flatMap((release) => [release.version, ...release.gameVersions, ...release.loaders])].join(" ").toLocaleLowerCase();
const render = (projects, query = "") => { const needle = query.trim().toLocaleLowerCase(); const visible = needle ? projects.filter((project) => searchableText(project).includes(needle)) : projects; projectsRoot.replaceChildren(); count.textContent = `${visible.length} 个模组`; if (!visible.length) return projectsRoot.append(element("p", "empty", projects.length ? "没有匹配的模组。" : "还没有发布模组。")); visible.forEach((project) => projectsRoot.append(renderProject(project))); };
try { const [configResponse, catalogResponse] = await Promise.all([fetch("/config.json"), fetch("/catalog.json", { cache: "no-cache" })]); if (!configResponse.ok || !catalogResponse.ok) throw new Error("无法读取站点数据"); const [config, catalog] = await Promise.all([configResponse.json(), catalogResponse.json()]); document.title = config.siteName; document.querySelector("#site-name").textContent = config.siteName; document.querySelector("#page-title").textContent = config.siteName; document.querySelector("#site-description").textContent = config.siteDescription; render(catalog.projects); search.addEventListener("input", () => render(catalog.projects, search.value)); } catch (error) { projectsRoot.replaceChildren(element("p", "error", `${error.message}，请稍后重试。`)); }
