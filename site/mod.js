import { renderMarkdown } from "./markdown.js";

const root = document.querySelector("#mod-page");
const element = (tag, className, text) => { const node = document.createElement(tag); if (className) node.className = className; if (text !== undefined) node.textContent = text; return node; };
const formatBytes = (bytes) => bytes < 1024 ** 2 ? `${(bytes / 1024).toFixed(1)} KB` : `${(bytes / 1024 ** 2).toFixed(1)} MB`;
const icon = (project) => { if (!project.icon) return element("span", "project-icon project-icon--fallback project-icon--large", project.name.slice(0, 1)); const image = element("img", "project-icon project-icon--large"); image.src = project.icon; image.alt = ""; return image; };
const renderFile = (file) => { const item = element("li", "download-file"); const info = element("div"); info.append(element("strong", "", file.name)); info.append(element("span", "download-file__meta", `${formatBytes(file.size)} · SHA-256 ${file.sha256.slice(0, 12)}…`)); const link = element("a", "download", "下载"); link.href = file.url; link.setAttribute("download", ""); item.append(info, link); return item; };
const renderRelease = (release, latest) => { const details = element("details", "release"); details.open = latest; const summary = element("summary"); summary.append(element("strong", "", release.version)); summary.append(element("span", "release__date", new Date(release.publishedAt).toLocaleDateString("zh-CN"))); if (latest) summary.append(element("span", "tag tag--accent", "最新版")); details.append(summary); const body = element("div", "release__body"); const tags = element("div", "tags"); [...release.gameVersions.map((version) => `游戏 ${version}`), ...release.loaders].forEach((value) => tags.append(element("span", "tag", value))); if (tags.childElementCount) body.append(tags); if (release.notes) body.append(element("p", "release__notes", release.notes)); const files = element("ul", "download-files"); release.files.forEach((file) => files.append(renderFile(file))); body.append(files); details.append(body); return details; };
const renderProject = (project) => { document.title = `${project.name} · ${document.querySelector("#site-name").textContent}`; root.replaceChildren(); const banner = element("section", "mod-banner"); if (project.banner) { const image = element("img", "mod-banner__image"); image.src = project.banner; image.alt = ""; banner.append(image); } else banner.classList.add("placeholder-banner"); const bannerContent = element("div", "mod-banner__content"); bannerContent.append(icon(project)); const heading = element("div"); heading.append(element("p", "eyebrow", "MOD OVERVIEW")); heading.append(element("h1", "", project.name)); const metaRow = element("div", "mod-banner__meta"); if (project.authors && project.authors.length) { metaRow.append(element("span", "mod-banner__authors", `作者：${project.authors.join("、")}`)); } if (project.source) { const sourceLink = element("a", "mod-banner__source", "源码仓库 ↗"); sourceLink.href = project.source; sourceLink.target = "_blank"; sourceLink.rel = "noopener noreferrer"; metaRow.append(sourceLink); } if (metaRow.childElementCount) heading.append(metaRow); if (project.description) heading.append(element("p", "mod-banner__summary", project.description)); bannerContent.append(heading); banner.append(bannerContent); root.append(banner); const layout = element("div", "mod-layout"); const article = element("article", "mod-introduction"); article.append(element("h2", "", "模组介绍")); const description = element("div", "long-description"); description.append(renderMarkdown(project.longDescription || project.description || "暂未填写介绍。")); article.append(description); layout.append(article); const aside = element("aside", "download-panel"); aside.append(element("h2", "", "下载文件")); if (!project.releases.length) aside.append(element("p", "empty", "暂时没有可下载的版本。")); project.releases.forEach((release, index) => aside.append(renderRelease(release, index === 0))); layout.append(aside); root.append(layout); };
try { const [configResponse, catalogResponse] = await Promise.all([fetch("/config.json"), fetch("/catalog.json", { cache: "no-cache" })]); if (!configResponse.ok || !catalogResponse.ok) throw new Error("无法读取站点数据"); const [config, catalog] = await Promise.all([configResponse.json(), catalogResponse.json()]); document.querySelector("#site-name").textContent = config.siteName; const project = catalog.projects.find((item) => item.slug === new URLSearchParams(location.search).get("project")); if (!project) throw new Error("找不到这个模组"); renderProject(project); } catch (error) { root.replaceChildren(element("p", "error", `${error.message}。`)); }
